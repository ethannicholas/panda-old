package org.pandalanguage.pandac.ast

uses org.pandalanguage.pandac.tree.Position

@abstract
class ASTType : ASTNode {
    def isNullable:Bit

    constructor(position:Position, isNullable:Bit) {
        super.constructor(position)
        self.isNullable := isNullable
    }
}