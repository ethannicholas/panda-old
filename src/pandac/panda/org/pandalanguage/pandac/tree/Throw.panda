package org.pandalanguage.pandac.tree

class Throw : Statement {
    def error:Value

    constructor(position:Position, error:Value) {
        super.constructor(position)
        self.error := error
    }
    
    @override
    function format(fmt:String):String {
        return "throw " + error
    }
}