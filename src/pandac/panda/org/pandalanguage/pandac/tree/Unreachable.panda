package org.pandalanguage.pandac.tree

class Unreachable : Statement {
    def error:Value?

    init(position:Position, error:Value?) {
        super.init(position)
        self.error := error
    }
}