package panda.core

================================================================================
The abstract superclass of the real number (floating point) types `Real32` and 
`Real64`.
================================================================================
@abstract
class RealNumber : Number {
	@override
	function english():String {
		throw NotSupportedException()
	}

	@override
	function roman():String {
		throw NotSupportedException()
	}
}