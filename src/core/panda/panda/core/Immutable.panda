package panda.core

================================================================================
Abstract superclass of all objects which are guaranteed to never change the
value of any of their fields. `Immutable` is handled specially by the compiler;
any field present in any object which descends from `Immutable` must itself be
`Immutable`, and is implied to be `@static`.
================================================================================
@abstract
class Immutable {
}