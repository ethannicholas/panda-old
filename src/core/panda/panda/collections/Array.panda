package panda.collections

================================================================================
A mutable list of values. `Array` is a generic class, instantiated to hold a
particular kind of object such as `Array<Int>` or `Array<String>`.
================================================================================
class Array {
    @private
    @class
    @external
    method setLength(a:Object, elementSize:Int32, reserved:Bit, length:Int)
}