package panda.collections

interface Collection<T> : CollectionView<T>, CollectionWriter<T> {
    method filterInPlace(f:(T)=>(Bit)) {
        for i in length - 1 ... 0 by -1 {
            if !f(self[i])
                removeIndex(i)
        }
    }
}