package panda.collections

interface CollectionWriter<T> {
    method add(object:T)

    method remove(object:T)

    method clear()

    method filter(f:(T)=>(Bit)) {
        throw new NotSupportedException()
    }
}