class Initialization {
    var s:String
    
    @class
    method main() {
        try {
            Initialization()
            Console.writeLine("failure")
        }
        catch e:AssertionError {
            Console.writeLine("success")
        }
    }
}