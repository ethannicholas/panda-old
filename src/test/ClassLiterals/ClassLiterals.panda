Console.writeLine(Int32)
Console.writeLine(Int32.name)
Console.writeLine(Int32.MAX)

Console.writeLine(panda.core.Int32)
Console.writeLine(panda.core.Int32.name)
Console.writeLine(panda.core.Int32.MAX)

var i1 := Int32
Console.writeLine(i1)

var i2 := Int32
Console.writeLine(i2)

Console.writeLine(ListView<Object>)
Console.writeLine(panda.collections.ListView<Object>)
