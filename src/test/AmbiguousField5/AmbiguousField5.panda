================================================================================
The reference to "t.length" is not actually ambiguous because the length method
is not a function, and methods cannot omit their parameter list.
================================================================================
class AmbiguousField {
    @private
    var length:Int := 5

    method length():Int {
        return 2
    }

    @class
    method main() {
        var t := AmbiguousField()
        Console.writeLine(t.length)
    }
}