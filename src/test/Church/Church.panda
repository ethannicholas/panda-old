@pre(x >= 0)
function church(x:Int):(Int)=>(Int) {
    if x = 0
        return f => x => x
    return sum(f:Int => f, church(x - 1))
}

function sum(a:(Int)=>(Int), b:(Int)=>(Int)):(Int)=>(Int) {
    return f:(Int)=>(Int) => b(f)(a(f))
}

