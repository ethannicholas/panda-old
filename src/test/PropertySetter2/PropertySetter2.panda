interface Interface {
    function length():Int
}

class Super (Interface) {
    var length:Int

    @override
    function length():Int {
        return length
    }
}

class Sub : Super {
    @class
    method main() {
        var s := new Sub()
        s.length := 0
        Console.writeLine("success")
    }
}