================================================================================
The seemingly-ambiguous reference to "t.length:=" isn't actually ambiguous 
because the field is @readonly.
================================================================================
class AmbiguousField8 {
    @readonly
    var length:Int := 5

    @self
    method length:=(length:Int) {
        Console.writeLine("called setter")
    }
}

method main() {
    var t := new AmbiguousField8()
    t.length := 0
    Console.writeLine(t.length)
}