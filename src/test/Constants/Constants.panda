class Constants {
    constant x := 5
    constant y:Int8 := 8

    @class
    method main() {
        assert x.class.name = "panda.core.Int32"
        assert y.class.name = "panda.core.Int8"
        constant z := "Hello, World!"
        Console.writeLine(z)
    }
}