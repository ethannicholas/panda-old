try {
    var a:Object := "Hello"
    var b := a->(MutableString)
}
catch e:CastError {
    Console.writeLine("ok")
}

try {
    var a:Object? := null
    var b := a->(Object)
}
catch e:CastError {
    Console.writeLine("ok")
}