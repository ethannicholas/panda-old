================================================================================
Seemingly-ambiguous reference to t.length isn't actually ambiguous, because the
function is public but the field is private.
================================================================================
class AmbiguousField7 {
    @private
    var length:Int := 5

    function length():Int {
        return 10
    }
}

method main() {
    var t := new AmbiguousField7()
    Console.writeLine(t.length)
}