package panda.ui.events

choice EventType {
	KEYUP,
	KEYDOWN,
    KEYREPEAT,
	MOUSEMOTION,
	MOUSEDOWN,
	MOUSEUP,
	UNKNOWN
}

class Event : Value {
	def type:EventType

    init(type:EventType) {
        self.type := type
    }
}