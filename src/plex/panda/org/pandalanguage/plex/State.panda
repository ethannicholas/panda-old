package org.pandalanguage.plex

@abstract
class State : Value {
    var next:ImmutableArray<Int>

    constructor(next:ImmutableArray<Int>) {
        self.next := next
    }

    @abstract
    function accept(c:Char):Bit
}