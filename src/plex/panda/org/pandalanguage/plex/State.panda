package org.pandalanguage.plex

@abstract
class State : Value {
    def next:ImmutablePrimitiveArray<Int>

    constructor(next:ImmutablePrimitiveArray<Int>) {
        self.next := next
    }

    @abstract
    function accept(c:Char):Bit
}