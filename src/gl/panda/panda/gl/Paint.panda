package panda.gl

uses panda.gl.shapes.Rectangle

================================================================================
Defines colors used for painting operations. Each `Paint` defines an unbounded
area of pixels, and each draw operation "cuts out" a selection of these pixels
and stamps them onto the screen.
================================================================================
interface Paint {
--	function getPixels(area:Rectangle):Texture
}