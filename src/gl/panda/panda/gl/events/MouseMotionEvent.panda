package panda.gl.events

uses panda.gl.Point

class MouseMotionEvent : Event {
	var position:Point

    constructor(type:EventType, position:Point) {
        super.constructor(type)
        self.position := position
    }
}