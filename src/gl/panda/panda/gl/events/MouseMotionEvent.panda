package panda.gl.events

uses panda.gl.Point

class MouseMotionEvent : Event {
	def position:Point

    init(type:EventType, position:Point) {
        super.init(type)
        self.position := position
    }
}