package panda.gl.events

enum EventType {
	KEYUP,
	KEYDOWN,
    KEYREPEAT,
	MOUSEMOTION,
	MOUSEDOWN,
	MOUSEUP,
	UNKNOWN
}

class Event : Value {
	def type:EventType

    init(type:EventType) {
        self.type := type
    }
}