package panda.gl.events

enum EventType {
	KEYUP,
	KEYDOWN,
    KEYREPEAT,
	MOUSEMOTION,
	MOUSEDOWN,
	MOUSEUP,
	UNKNOWN
}

class Event : Value {
	var type:EventType

    constructor(type:EventType) {
        self.type := type
    }
}