package panda.gl

uses panda.gl.shapes.Rectangle

================================================================================
A repeating texture created from an image.
================================================================================
class Texture (Paint) {
	@private
	var native:$NativePointer

	@readonly
	var width:Int

	@readonly
	var height:Int

	@private
	@external
	method close()

	@override
	method finalize() {
		close()
	}
}